<% pageTitle = '홈' %>

<div class="hero-section">
    <div class="container">
        <div class="hero-content">
            <h1 class="hero-title"><%= book.title %></h1>
            <h2 class="hero-subtitle"><%= book.subtitle %></h2>
            <p class="hero-description"><%= book.description %></p>
            <div class="hero-actions">
                <a href="/chapters" class="btn btn-primary">목차 보기</a>
                <a href="/about" class="btn btn-secondary">책 소개</a>
            </div>
        </div>
        <div class="hero-image">
            <div class="book-mockup">
                <div class="book-cover">
                    <h3>홀로서기<br>심리학</h3>
                    <p>진정한 나를 찾아가는<br>심리적 독립의 여정</p>
                </div>
            </div>
        </div>
    </div>
</div>

<section class="featured-chapters">
    <div class="container">
        <h2 class="section-title">주요 챕터</h2>
        <div class="chapters-grid">
            <% book.chapters.slice(0, 3).forEach(chapter => { %>
            <div class="chapter-card">
                <div class="chapter-number">Chapter <%= chapter.id %></div>
                <h3 class="chapter-title"><%= chapter.title %></h3>
                <p class="chapter-subtitle"><%= chapter.subtitle %></p>
                <p class="chapter-summary"><%= chapter.summary %></p>
                <a href="/chapter/<%= chapter.id %>" class="chapter-link">자세히 보기 →</a>
            </div>
            <% }); %>
        </div>
    </div>
</section>

<section class="book-benefits">
    <div class="container">
        <h2 class="section-title">이 책을 읽고 나면</h2>
        <div class="benefits-grid">
            <% book.benefits.forEach(benefit => { %>
            <div class="benefit-item">
                <div class="benefit-icon">✓</div>
                <p><%= benefit %></p>
            </div>
            <% }); %>
        </div>
    </div>
</section>

<section class="target-audience">
    <div class="container">
        <h2 class="section-title">이런 분들께 추천합니다</h2>
        <div class="audience-grid">
            <% book.targetAudience.forEach(audience => { %>
            <div class="audience-item">
                <div class="audience-icon">👤</div>
                <p><%= audience %></p>
            </div>
            <% }); %>
        </div>
    </div>
</section>

<section class="cta-section">
    <div class="container">
        <div class="cta-content">
            <h2>심리적 독립의 여정을 시작하세요</h2>
            <p>진정한 나를 찾아가는 여행에 지금 동참하세요.</p>
            <a href="/chapters" class="btn btn-primary btn-large">목차 살펴보기</a>
        </div>
    </div>
</section>